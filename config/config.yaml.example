# LongBridge API配置
longbridge:
  app_key: ""  # 从LongBridge开放平台获取
  app_secret: ""  # 从LongBridge开放平台获取
  access_token: ""  # 从LongBridge开放平台获取，可通过Web UI更新

# Polygon.io API配置（用于期权数据）
polygon:
  api_key: ""  # 从 https://polygon.io 获取，用于查询期权价格

# 邮件通知配置
email:
  smtp_host: "smtp.gmail.com"  # SMTP服务器地址
  smtp_port: 587  # SMTP端口
  smtp_user: ""  # SMTP用户名
  smtp_password: ""  # SMTP密码（建议使用应用专用密码）
  from_email: ""  # 发件人邮箱
  to_emails:  # 收件人邮箱列表
    - "your-email@example.com"

# Web服务配置
web:
  host: "0.0.0.0"  # 监听地址
  port: 5000  # 监听端口
  secret_key: ""  # Flask session密钥（随机字符串，用于安全）
  update_password: ""  # Token更新页面密码

# Google Sheets -> BoolReminder 交易同步
trade_sync:
  enabled: true
  bearer_token: "replace-with-a-random-secret"
  allowed_spreadsheet_ids: []  # 可选：限制允许推送的 Google Sheets ID
  default_sheet_name: "main"
  cache_ttl_minutes: 30
  max_sync_rows: 20000
